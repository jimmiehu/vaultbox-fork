{
  "name": "vaultbox",
  "version": "1.0.3",
  "description": "Sync your vault directly with a selected Dropbox folder on desktop and mobile.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "test:e2e": "node e2e/dropbox-e2e.mjs && vitest run --config vitest.e2e.config.ts",
    "dropbox:token": "node scripts/dropbox-refresh-token.mjs",
    "screenshot:settings": "npm run build && node e2e/settings-screenshot.mjs",
    "local-install": "npm run build && node local-install.mjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "dropbox",
    "sync"
  ],
  "author": "grumpydev",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.28.0",
    "obsidian": "^1.5.12",
    "playwright-core": "^1.60.0",
    "tslib": "^2.6.2",
    "typescript": "^5.4.5",
    "vitest": "^4.1.6"
  }
}
